function columnCount(id, columnCount) {
  document.getElementById(id).style.columnCount = columnCount;
  document.getElementsByClassName('text')[0].innerHTML = columnCount;
}
function columnGap(id, columnGap) {
  document.getElementById(id).style.columnGap = columnGap;
  document.getElementsByClassName('text')[0].innerHTML = columnGap;
}

function columnFill(id, columnFill) {
  document.getElementById(id).style.columnFill = columnFill;
  document.getElementsByClassName('text')[0].innerHTML = columnFill;
}

function columnSpan(id, columnSpan) {
  document.getElementById(id).style.columnSpan = columnSpan;
  document.getElementsByClassName('text')[0].innerHTML = columnSpan;
}

function columnWidth(id, columnWidth) {
  document.getElementById(id).style.columnWidth = columnWidth;
  document.getElementsByClassName('text')[0].innerHTML = columnWidth;
}

function columnRuleWidth(id, columnRuleWidth) {
  document.getElementById(id).style.columnRuleWidth = columnRuleWidth;
  document.getElementsByClassName('text')[0].innerHTML = columnRuleWidth;
}

function columnRuleStyle(id, columnRuleStyle) {
  document.getElementById(id).style.columnRuleStyle = columnRuleStyle;
  document.getElementsByClassName('text')[0].innerHTML = columnRuleStyle;
}

function columnRuleColor(id, columnRuleColor) {
  document.getElementById(id).style.columnRuleColor = columnRuleColor;
  document.getElementsByClassName('text')[0].innerHTML = columnRuleColor;
}

function columnRule(id, columnRule) {
  document.getElementById(id).style.columnRule = columnRule;
  document.getElementsByClassName('text')[0].innerHTML = columnRule;
}

function columns(id, columns) {
  document.getElementById(id).style.columns = columns;
  document.getElementsByClassName('text')[0].innerHTML = columns;
}
